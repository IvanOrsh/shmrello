import { createTheme, Theme } from "@mui/material";

const theme: Theme = createTheme({
  palette: {
    mode: "dark",
    background: {
      // default: "#BEA4FF"
      default: "#1D1F26",
    },
  },
});

export default theme;
